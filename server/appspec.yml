version: 0.0
os: linux
files:
  - source:  /
    destination: /home/ubuntu/server
    overwrite: yes

permissions:
  - object: /home/ubuntu/server
    pattern: "**"
    owner: root
    group: root

hooks:
  ApplicationStart:
    - location: scripts/deploy.sh
      timeout: 60
      runas: root
